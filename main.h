#ifndef MAIN_H
# define MAIN_H

/*
 * The entry point. Takes an array of arguments and their count as parameters.
 * Returns an integer indicating wether the program exited properly or that an
 * error occured, indicating which kind of error in that case.
 */
int	main(int argc, char **argv);

#endif
