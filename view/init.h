#ifndef INIT_H
# define INIT_H

/*
 * Initializes the view of this application. Returns a pointer to the
 * initialized view. After calling this function, the controls goes back after
 * everything that is needed is done.
 */
void	*init_mlx(void);

#endif
